# Tracekit
An open-source TypeScript audit logging toolkit for modern applications. Record who changed what and when, store before/after values and metadata, ignore sensitive fields, query audit events and integrate with Neon Postgres through direct SQL.
